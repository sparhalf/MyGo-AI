# 图形化界面（Tkinter）使用说明

本项目提供 `gui.py` 作为围棋对弈的图形化界面（仅依赖 Python 标准库 `tkinter`）。

## 功能支持

- **启动选择页**
  - 进入后先选择：**人机对弈** 或 **AI 对弈**
  - 选择完成后进入棋盘页面

- **人机对弈**
  - 默认：**玩家执黑先行**
  - 玩家需选择 AI 类型（4 选 1）：`random` / `mcts_std` / `mcts_enh` / `minimax`
  - 玩家通过鼠标点击落子，AI 会自动应手一步
  - 支持 **认输**
  - 若玩家点击了非法位置（不能落子），会提示 **“非法落子，请重下”**
  - 若当前无任何合法落子点（只剩 `pass/resign`），会提示需要 **Pass**

- **AI 对弈**
  - 选择黑方/白方的 Agent 类型（均为 4 选 1）
  - **不会自动连续走子**
  - 需要用户点击 **“下一步”**，AI 才走一步（便于观察）
  - AI 对弈模式下 **不提供 Pass / 悔棋 / 认输**（按钮禁用）

- **对局信息显示**
  - 显示 **当前回合（黑/白 + 人类/AI）**
  - 显示 **提子数（黑/白）**
  - 显示 **回合数（已下步数）** 与 **本局用时**

- **对局控制**
  - **新游戏**：按当前模式与已选择的 agent 重新开始
  - **悔棋**：
    - 人机对弈：默认最多回退两步，尽量回到人类回合
    - AI 对弈：该功能禁用
  - **Pass**：仅人机对弈时可用（玩家停一手）
  - **返回**：回到启动选择页

## 使用方法

在项目根目录运行：

```bash
python gui.py
```

指定棋盘大小（默认 5）：

```bash
python gui.py --size 5
```

## 备注

- GUI 中 `mcts_enh` 使用增强版参数（用于演示）：`num_rounds=200`、`time_limit_s=2.0`、`rollout_depth=15`；
  `mcts_std` 为标准版（`use_enhancements=False`）。
- 若你的 Python 环境没有自带 Tkinter，需要安装带 Tk 支持的 Python 发行版（多数 Windows/macOS/Linux 桌面版 Python 默认都包含）。

